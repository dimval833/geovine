import { defineConfig } from 'astro/config';

export default defineConfig({
  // Εδώ βάζεις το νέο σου domain
  site: 'https://www.geovine.gr',
  
  // Το base πρέπει να γίνει σκέτη κάθετος (ή να διαγραφεί τελείως η γραμμή)
  base: '/',
});
